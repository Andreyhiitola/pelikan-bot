# 🔌 Интеграция системы заказов с сайтом Пеликан Алаколь

## 📋 Что нужно сделать

### 1. Скопировать файлы на сайт

#### Основные файлы бара:
```
pelikan-alakol-site_v2/
├── bar.html                     ← Замените существующий
├── barzakaz.json                ← Обновите меню
└── js/
    └── bar.js                   ← Замените существующий
```

#### Дополнительные изображения (опционально):
```
pelikan-alakol-site_v2/
└── img/
    ├── placeholder.jpg          ← Изображение-заглушка
    ├── burger-chernyy.jpg
    ├── pizza-pepperoni.jpg
    ├── khachapuri.jpg
    └── ...другие блюда
```

### 2. Обновить конфигурацию в bar.js

Откройте `js/bar.js` и измените URL API:

```javascript
const CONFIG = {
    // Для production (после установки бота на VPS):
    API_URL: 'https://bar.pelikan-alakol.kz/api/order',
    
    // Для локальной разработки:
    // API_URL: 'http://localhost:8080/api/order',
    
    MENU_JSON: 'barzakaz.json'
};
```

### 3. Настроить Nginx на VPS

После установки бота добавьте в конфигурацию Nginx:

```nginx
server {
    listen 443 ssl http2;
    server_name bar.pelikan-alakol.kz;

    # SSL сертификаты
    ssl_certificate /etc/letsencrypt/live/bar.pelikan-alakol.kz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bar.pelikan-alakol.kz/privkey.pem;

    # Корневая директория сайта
    root /var/www/pelikan-alakol-site_v2;
    index index.html;

    # Статические файлы сайта
    location / {
        try_files $uri $uri/ =404;
    }

    # API для заказов (проксирование на webhook сервер)
    location /api/order {
        proxy_pass http://127.0.0.1:8080/api/order;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # CORS (если нужно)
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'POST, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Content-Type' always;
        
        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }
}
```

### 4. Добавить ссылку на меню в главном сайте

В вашем `index.html` уже есть кнопка меню:

```html
<!-- 6. МЕНЮ -->
<a href="bar.html" class="header-btn menu-btn">
    <i class="fas fa-utensils"></i> Меню
</a>
```

Это правильно! Просто убедитесь, что ссылка ведёт на `bar.html`, а не `index_menu.html`.

### 5. Обновить структуру barzakaz.json

Ваш JSON должен быть массивом блюд с такой структурой:

```json
[
  {
    "id": "уникальный-идентификатор",
    "name": "Название блюда",
    "description": "Описание (опционально)",
    "category": "Категория",
    "price": 2500,
    "image": "имя-файла-картинки.jpg"
  }
]
```

**Пример:**

```json
[
  {
    "id": "burger-1",
    "name": "Бургер Черный",
    "description": "Сочная говяжья котлета с черной булочкой",
    "category": "Бургеры",
    "price": 2500,
    "image": "burger-chernyy.jpg"
  },
  {
    "id": "pizza-1",
    "name": "Пицца Пепперони",
    "description": "Классическая пицца с пепперони",
    "category": "Пицца",
    "price": 3500,
    "image": "pizza-pepperoni.jpg"
  }
]
```

## 📝 Полный Checklist установки

### На VPS:

- [ ] 1. Установить Telegram бота (см. QUICKSTART.md)
- [ ] 2. Запустить webhook сервер (`pelikan-webhook.service`)
- [ ] 3. Настроить Nginx для проксирования API
- [ ] 4. Получить SSL сертификат для домена
- [ ] 5. Проверить доступность `https://bar.pelikan-alakol.kz/api/order`

### На сайте:

- [ ] 6. Скопировать новые файлы (`bar.html`, `bar.js`, `barzakaz.json`)
- [ ] 7. Обновить меню в `barzakaz.json`
- [ ] 8. Добавить изображения блюд в `img/`
- [ ] 9. Проверить ссылку на меню в `index.html`
- [ ] 10. Обновить `API_URL` в `bar.js`

### Тестирование:

- [ ] 11. Открыть `bar.html` в браузере
- [ ] 12. Добавить блюда в корзину
- [ ] 13. Оформить тестовый заказ
- [ ] 14. Проверить получение уведомления в Telegram боте
- [ ] 15. Проверить статус заказа командой `/status`

## 🔧 Решение проблем

### Меню не загружается

**Проблема:** Пустая страница или ошибка "Ошибка загрузки меню"

**Решение:**
1. Проверьте путь к `barzakaz.json`
2. Откройте консоль браузера (F12) и посмотрите ошибки
3. Убедитесь, что JSON файл валидный (используйте [jsonlint.com](https://jsonlint.com))

### Заказ не отправляется

**Проблема:** Ошибка при нажатии "Оформить заказ"

**Решение:**
1. Проверьте `API_URL` в `bar.js`
2. Убедитесь, что webhook сервер запущен:
   ```bash
   sudo systemctl status pelikan-webhook
   ```
3. Проверьте логи:
   ```bash
   sudo journalctl -u pelikan-webhook -f
   ```
4. Проверьте доступность API:
   ```bash
   curl https://bar.pelikan-alakol.kz/api/order
   ```

### Бот не получает заказы

**Проблема:** Заказ отправлен, но бот не уведомляет

**Решение:**
1. Проверьте статус бота:
   ```bash
   sudo systemctl status pelikan-bot
   ```
2. Проверьте логи бота:
   ```bash
   sudo journalctl -u pelikan-bot -f
   ```
3. Убедитесь, что токен бота правильный в `.env`
4. Проверьте ADMIN_IDS в `.env`

### Изображения не отображаются

**Проблема:** Вместо картинок показывается placeholder

**Решение:**
1. Убедитесь, что изображения находятся в `img/`
2. Проверьте названия файлов (должны совпадать с `image` в JSON)
3. Добавьте `placeholder.jpg` для блюд без картинок

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи:
   ```bash
   # Бот
   sudo journalctl -u pelikan-bot -n 100
   
   # Webhook
   sudo journalctl -u pelikan-webhook -n 100
   
   # Nginx
   sudo tail -f /var/log/nginx/error.log
   ```

2. Запустите тест:
   ```bash
   cd ~/telegram_bot_pelican_alacol
   ./scripts/test.sh
   ```

3. Создайте Issue на GitHub с описанием проблемы и логами

## 🎨 Кастомизация

### Изменить дизайн

Все стили находятся в `<style>` секции `bar.html`. Вы можете изменить:
- Цвета (переменные в начале)
- Размеры карточек
- Шрифты
- Анимации

### Добавить новые поля в форму

1. Добавьте поле в `bar.html`:
```html
<div class="form-group">
    <label for="phone">Телефон</label>
    <input type="tel" id="phone" name="phone" placeholder="+7 777 123 45 67">
</div>
```

2. Обновите `handleOrderSubmit` в `bar.js`:
```javascript
const orderData = {
    // ...
    phone: formData.get('phone').trim(),
    // ...
};
```

3. Обновите `bot.py` для обработки нового поля

## ✅ Готово!

После выполнения всех шагов ваша система заказов будет полностью интегрирована с сайтом и Telegram ботом!

**Тестовый заказ:**
1. Откройте `https://bar.pelikan-alakol.kz` (или ваш домен)
2. Выберите блюда
3. Оформите заказ
4. Проверьте уведомление в Telegram

---

**Вопросы?** Создайте Issue на GitHub или свяжитесь с поддержкой.
