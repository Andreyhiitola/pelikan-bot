# 👨‍💼 Инструкция для администратора

## Полный доступ ко всем функциям

### 📱 Команды в Telegram

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню |
| `/admin_panel` | Админ-панель |
| `/stats` | Статистика за день |
| `/backup` | Скачать бэкап БД |
| `/admin_reviews` | Модерация отзывов |

### 🔧 SSH доступ к серверу
```bash
ssh root@85.192.40.138
```

**Пароль:** (отдельно)

### 📝 Редактирование отзывов
```bash
python3 ~/pelikan-bot/edit_reviews.py
```

**Меню:**
1. Показать все отзывы
2. Посмотреть отзыв (по ID)
3. Редактировать отзыв
4. Удалить отзыв
0. Выход

**Что можно редактировать:**
- Отображаемое имя
- Номер комнаты
- Плюсы/минусы
- Комментарий
- Публикация (да/нет)
- Статус (pending/approved/rejected)

### 📦 Редактирование заказов
```bash
python3 ~/pelikan-bot/edit_orders.py
```

**Меню:**
1. Показать заказы (все/активные/сегодня)
2. Посмотреть заказ
3. Редактировать заказ
4. Удалить заказ
5. Статистика и аналитика
6. Очистка старых заказов
0. Выход

**Статистика включает:**
- За день/неделю/месяц
- Топ-10 клиентов
- Средний чек
- По статусам

### 💾 Бэкапы

**Автоматические:**
- Каждый день в 8:00 (время Алматы)
- Хранятся 7 дней

**Ручной бэкап через Telegram:**
```
/backup
```

**Ручной бэкап через SSH:**
```bash
~/pelikan-bot/backup.sh
```

**Восстановление:**
```bash
docker-compose down
gunzip -c ~/pelikan-bot/backups/daily/orders_daily_YYYYMMDD.db.gz > ~/pelikan-bot/data/orders.db
docker-compose up -d
```

### 👥 Управление пользователями

**Добавить нового пользователя:**
```bash
nano ~/pelikan-bot/.env
```

Добавить Telegram ID в нужную роль:
```
ADMIN_IDS=31310268,1299489409,НОВЫЙ_ID
MANAGER_IDS=8027468814,8221991159,НОВЫЙ_ID
WAITER_IDS=2045389365,НОВЫЙ_ID
```

Сохранить и перезапустить:
```bash
docker-compose restart pelikan-bot
```

### 🔄 Перезапуск бота
```bash
cd ~/pelikan-bot
docker-compose restart pelikan-bot

# Проверить логи
docker logs -f pelikan-bot
```

### 📊 Прямая работа с БД
```bash
sqlite3 ~/pelikan-bot/data/orders.db
```

**Полезные команды:**
```sql
-- Посмотреть все отзывы
SELECT id, guest_name, status, is_published FROM reviews;

-- Опубликовать отзыв
UPDATE reviews SET is_published=1, status='approved' WHERE id=5;

-- Статистика заказов
SELECT status, COUNT(*), SUM(total) FROM orders GROUP BY status;

-- Выход
.exit
```

### 🆘 Решение проблем

**Бот не отвечает:**
```bash
docker logs pelikan-bot
docker-compose restart pelikan-bot
```

**База данных повреждена:**
```bash
# Восстановить из бэкапа
docker-compose down
cp ~/pelikan-bot/backups/daily/orders_daily_*.db.gz /tmp/
gunzip /tmp/orders_daily_*.db.gz
mv /tmp/orders_daily_*.db ~/pelikan-bot/data/orders.db
docker-compose up -d
```

**Обновить код:**
```bash
cd ~/pelikan-bot
git pull origin main
docker-compose down
docker-compose up -d --build
```

---

## 📞 Контакты

**Поддержка:** @pelikan_alakol_support
**Админ:** Мария +77056507808
**Сервер:** 85.192.40.138

**Полная документация:** USER_GUIDE.md
