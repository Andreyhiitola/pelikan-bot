# 🚀 ОБНОВЛЕНИЕ БОТА С МОДУЛЕМ АНАЛИТИКИ

## Файлы для копирования на сервер:

```
pelikan-hotel-bot/
├── analytics_handler.py      ← НОВЫЙ
├── analytics_commands.py     ← НОВЫЙ
├── bot.py                    ← ОБНОВИТЬ (добавить 3 строки)
├── requirements.txt          ← ОБНОВИТЬ (новые зависимости)
├── Dockerfile                ← ОБНОВИТЬ (копировать новые модули)
├── docker-compose.yml        (без изменений)
├── .env                      ← ОБНОВИТЬ (SMTP настройки)
└── reviews_handler.py        (без изменений)
```

---

## 📋 ШАГ 1: Скопируйте файлы на сервер

```bash
# На локальной машине (из директории с загруженными файлами)
scp analytics_handler.py analytics_commands.py requirements.txt Dockerfile \
    root@ваш_сервер:/root/pelikan-hotel-bot/
```

Или скопируйте вручную через FileZilla/WinSCP.

---

## 📋 ШАГ 2: Обновите .env на сервере

SSH на сервер:
```bash
ssh root@ваш_сервер
cd /root/pelikan-hotel-bot
nano .env
```

Добавьте в конец файла:
```bash
# Email настройки для отчетов
SMTP_SERVER=smtp.mail.ru
SMTP_PORT=587
SMTP_USER=your_email@mail.ru
SMTP_PASSWORD=ваш_пароль_приложения
REPORT_EMAIL=regsk@mail.ru
```

**ВАЖНО:** Для Mail.ru нужен **пароль приложения**:
1. Зайдите на Mail.ru
2. Настройки → Пароль и безопасность
3. Пароли для внешних приложений → Создать новый
4. Скопируйте пароль и вставьте в .env

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

## 📋 ШАГ 3: Обновите bot.py

```bash
nano bot.py
```

### 3.1. Добавьте импорты (в начало, после других импортов):

```python
from analytics_handler import setup_scheduler
from analytics_commands import analytics_router
```

### 3.2. Подключите роутер (после других роутеров):

```python
# Подключаем роутеры
dp.include_router(reviews_router)
dp.include_router(analytics_router)  # ← ДОБАВЬТЕ
```

### 3.3. Запустите планировщик (в функции main, после await init_db()):

```python
async def main():
    # Инициализация базы данных
    await init_db()
    print("✅ База данных инициализирована")
    
    # ДОБАВЬТЕ ЭТИ 3 СТРОКИ:
    scheduler = setup_scheduler(bot)
    scheduler.start()
    print("✅ Планировщик отчетов запущен (отправка в 8:00)")
    
    # ... остальной код ...
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

## 📋 ШАГ 4: Пересоберите и запустите

```bash
cd /root/pelikan-hotel-bot

# Остановите бота
docker-compose down

# Пересоберите образ (ОБЯЗАТЕЛЬНО!)
docker-compose build --no-cache

# Запустите
docker-compose up -d

# Проверьте логи
docker logs -f pelikan-bot
```

Должны увидеть:
```
✅ База данных инициализирована
✅ Планировщик отчетов запущен (отправка в 8:00)
✅ Webhook установлен: ...
```

---

## 📋 ШАГ 5: Тестирование

Откройте бота в Telegram и отправьте:
```
/test_report
```

Должны получить:
- ✅ Текстовый отчет в Telegram
- ✅ 3 графика (динамика, категории, распределение)
- ✅ Email на regsk@mail.ru

---

## 🔍 Проверка автоматической отправки

Отчеты будут приходить **каждый день в 8:00** (время Алматы).

Чтобы проверить, что планировщик работает:
```bash
docker logs pelikan-bot | grep "Планировщик"
```

Должно быть:
```
✅ Планировщик отчетов запущен (отправка в 8:00)
```

---

## 🔄 GitHub Actions (автоматический деплой)

Если используете CI/CD через GitHub:

1. Закоммитьте изменения:
```bash
git add analytics_handler.py analytics_commands.py requirements.txt Dockerfile bot.py
git commit -m "Добавлен модуль аналитики отзывов"
git push
```

2. GitHub Actions автоматически:
   - Соберет новый образ
   - Отправит на Docker Hub
   - Watchtower на сервере подхватит обновление через 5 минут

3. НЕ ЗАБУДЬТЕ обновить .env на сервере вручную!

---

## ❌ Решение проблем

### Ошибка: "ModuleNotFoundError: No module named 'matplotlib'"
```bash
# Пересоберите образ заново
docker-compose build --no-cache
docker-compose up -d
```

### Ошибка: Email не отправляется
```bash
# Проверьте .env:
cat .env | grep SMTP

# Проверьте логи:
docker logs pelikan-bot | grep -i email
docker logs pelikan-bot | grep -i smtp
```

### Ошибка: "No module named 'analytics_handler'"
```bash
# Убедитесь, что файлы скопированы:
ls -la analytics_*

# Пересоберите:
docker-compose build --no-cache
docker-compose up -d
```

### Проверка работы планировщика
```bash
# Посмотрите все логи с момента запуска:
docker logs pelikan-bot

# Должна быть строка:
# ✅ Планировщик отчетов запущен (отправка в 8:00)
```

---

## ✅ Команды для админов

После установки доступны команды:

- `/analytics` - меню аналитики (выбор периода)
- `/test_report` - тестовая отправка отчета
- `/admin_reviews` - модерация отзывов (как раньше)
- `/all_reviews` - управление отзывами (как раньше)

---

## 📊 Что дальше?

Теперь каждый день в 8:00:
- 📱 Telegram-отчет всем админам (указанным в ADMIN_IDS)
- 📧 Email на regsk@mail.ru

Отчет включает:
- Средние оценки и тренды
- Распределение по категориям
- Проблемные зоны
- Лучшие и худшие отзывы
- 3 графика

---

## 🎉 Готово!

Ваш бот теперь с автоматической аналитикой отзывов!
