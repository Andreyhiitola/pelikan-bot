# 🚀 GIT КОМАНДЫ ДЛЯ ДОБАВЛЕНИЯ АНАЛИТИКИ

## ⚡ БЫСТРЫЙ СТАРТ

```bash
cd ~/Desktop/pelikan-hotel-bot

# 1. Создать новую ветку
git checkout -b feature/analytics

# 2. Скопировать файлы аналитики в директорию проекта
# (analytics_handler.py, analytics_commands.py, requirements.txt, Dockerfile)

# 3. Обновить bot.py (добавить 3 строки - см. bot_integration_example.py)

# 4. Обновить .env (добавить SMTP)

# 5. Добавить в Git
git add analytics_handler.py analytics_commands.py requirements.txt Dockerfile bot.py
git status

# 6. Закоммитить
git commit -m "feat: добавлен модуль аналитики отзывов"

# 7. Протестировать
docker-compose down
docker-compose build --no-cache
docker-compose up -d
docker logs -f pelikan-bot

# 8. Если работает - мердж в main
git checkout main
git merge feature/analytics
git push origin main
```

---

## 🌿 АВТОМАТИЧЕСКИЙ СКРИПТ

Используйте готовый скрипт:

```bash
cd ~/Desktop/pelikan-hotel-bot

# Скопируйте файлы аналитики в директорию

# Запустите скрипт
bash setup_analytics_branch.sh
```

Скрипт автоматически:
- ✅ Создаст ветку feature/analytics
- ✅ Проверит наличие файлов
- ✅ Проверит интеграцию в bot.py
- ✅ Проверит настройки .env
- ✅ Добавит файлы в Git
- ✅ Предложит закоммитить

---

## 📋 ОСНОВНЫЕ КОМАНДЫ

### Просмотр:
```bash
git status              # Статус изменений
git branch              # Список веток (* = текущая)
git log --oneline       # История коммитов
git diff                # Изменения в файлах
```

### Работа с ветками:
```bash
git checkout -b feature/analytics  # Создать и переключиться
git checkout main                  # Вернуться на main
git branch -d feature/analytics    # Удалить ветку
```

### Коммиты:
```bash
git add .                          # Добавить все изменения
git add filename                   # Добавить конкретный файл
git commit -m "описание"          # Закоммитить
git commit --amend                 # Изменить последний коммит
```

### Мердж:
```bash
git checkout main                  # Переключиться на main
git merge feature/analytics        # Смердить ветку
git push origin main               # Отправить на GitHub
```

---

## ⚠️ ВАЖНО: .env НЕ В GIT!

```bash
# Проверить, что .env не в Git
git status | grep .env

# Если .env попал в Git - удалить
git rm --cached .env
echo ".env" >> .gitignore
git add .gitignore
git commit -m "Добавлен .env в .gitignore"
```

---

## 🔄 ОТКАТ ИЗМЕНЕНИЙ

### Отменить изменения в файле (не закоммичено):
```bash
git checkout -- filename
```

### Вернуться на main (без изменений):
```bash
git checkout main
```

### Удалить ветку (если не нужна):
```bash
git branch -D feature/analytics
```

---

## 🧪 ТЕСТИРОВАНИЕ

После создания ветки и коммита:

```bash
# Пересобрать Docker
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Проверить логи
docker logs -f pelikan-bot

# Тестировать в Telegram
/test_report
```

Если всё работает:
```bash
git checkout main
git merge feature/analytics
git push origin main
```

---

## 📊 ПРОВЕРКА ПЕРЕД МЕРДЖЕМ

```bash
# На ветке feature/analytics
git status                         # Всё закоммичено?
docker logs pelikan-bot | grep "✅" # Планировщик запущен?

# В Telegram
/test_report                       # Отчет получен?
                                   # Email пришел?
```

---

## 🎯 РЕКОМЕНДУЕМЫЙ WORKFLOW

```
1. main (production) → создать ветку
2. feature/analytics → внести изменения
3. feature/analytics → закоммитить
4. feature/analytics → протестировать
5. main ← мердж (если работает)
6. GitHub → push
7. Watchtower → автодеплой (через 5 мин)
```

---

## 💡 СОВЕТЫ

- Всегда работайте в отдельной ветке для новых фич
- Коммитьте часто, по логическим блокам
- Тестируйте перед мерджем в main
- .env никогда не должен быть в Git
- Используйте осмысленные сообщения коммитов
