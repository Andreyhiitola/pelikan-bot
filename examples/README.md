# 📁 Examples - Примеры интеграции

Эта папка содержит примеры файлов для интеграции системы заказов с вашим веб-сайтом.

## 📄 Файлы

### bar.html
Полная HTML страница меню бара с:
- Отображением меню по категориям
- Корзиной заказов
- Формой оформления заказа
- Адаптивным дизайном
- Интеграцией с Telegram ботом

**Использование:**
```bash
# Скопируйте на ваш сайт
cp bar.html /var/www/pelikan-alakol-site_v2/
```

### bar.js
JavaScript код для:
- Загрузки меню из JSON
- Управления корзиной
- Отправки заказов на сервер
- Отображения уведомлений

**Использование:**
```bash
# Скопируйте в папку js вашего сайта
cp bar.js /var/www/pelikan-alakol-site_v2/js/
```

**Конфигурация:**
```javascript
const CONFIG = {
    API_URL: 'https://bar.pelikan-alakol.kz/api/order', // Измените на ваш домен
    MENU_JSON: 'barzakaz.json'
};
```

### bar-integration.js *(альтернативный вариант)*
Упрощённая версия интеграции с дополнительными функциями:
- localStorage для сохранения корзины
- Модальные окна
- Расширенная валидация

### barzakaz.json
Пример файла меню с блюдами:
```json
[
  {
    "id": "burger-1",
    "name": "Бургер Черный",
    "description": "Описание блюда",
    "category": "Бургеры",
    "price": 2500,
    "image": "burger-chernyy.jpg"
  }
]
```

**Структура:**
- `id` - уникальный идентификатор (обязательно)
- `name` - название блюда (обязательно)
- `description` - описание (опционально)
- `category` - категория для группировки (обязательно)
- `price` - цена в тенге (обязательно)
- `image` - имя файла изображения (опционально)

### bar-checkout-example.html *(устаревший)*
Старая версия с отдельной формой оформления. Используйте вместо неё `bar.html`.

## 🚀 Быстрый старт

### 1. Скопируйте файлы на сайт

```bash
# На вашем VPS
cd /var/www/pelikan-alakol-site_v2

# Скопируйте HTML
cp ~/telegram_bot_pelican_alacol/examples/bar.html ./

# Скопируйте JS
cp ~/telegram_bot_pelican_alacol/examples/bar.js ./js/

# Скопируйте JSON
cp ~/telegram_bot_pelican_alacol/examples/barzakaz.json ./
```

### 2. Обновите меню

Отредактируйте `barzakaz.json`:
```bash
nano barzakaz.json
```

Добавьте ваши блюда в формате JSON.

### 3. Добавьте изображения

```bash
# Создайте папку для картинок (если нет)
mkdir -p img

# Добавьте изображения блюд
# Названия должны совпадать с полем "image" в JSON
cp burger-chernyy.jpg img/
cp pizza-pepperoni.jpg img/
# и т.д.
```

**Заглушка для блюд без картинок:**
```bash
# Создайте простое изображение-заглушку
# или скачайте готовое:
wget https://via.placeholder.com/400x300/333/FFD700?text=No+Image -O img/placeholder.jpg
```

### 4. Обновите конфигурацию

В файле `js/bar.js` измените URL:
```javascript
const CONFIG = {
    API_URL: 'https://bar.pelikan-alakol.kz/api/order', // ВАШ ДОМЕН
    MENU_JSON: 'barzakaz.json'
};
```

### 5. Проверьте работу

Откройте в браузере:
```
https://ваш-домен.kz/bar.html
```

## 🔧 Кастомизация

### Изменить цвета

В `bar.html` найдите раздел `<style>` и измените переменные:

```css
body {
    background: #000 url('img/bg-wood.jpg'); /* Фон */
    color: #fff;
}

h2 {
    color: #FFD700; /* Цвет заголовков */
}

.dish-card {
    border: 2px solid rgba(255, 215, 0, 0.3); /* Рамка карточек */
}
```

### Добавить новые категории

Просто добавьте блюда с новой категорией в `barzakaz.json`:
```json
{
    "id": "drink-1",
    "name": "Кофе Американо",
    "category": "Напитки", // Новая категория
    "price": 400
}
```

Категории отобразятся автоматически!

### Изменить форму заказа

В `bar.html` найдите `<form id="order-form">` и добавьте поля:

```html
<div class="form-group">
    <label for="phone">Телефон</label>
    <input type="tel" id="phone" name="phone">
</div>
```

Затем обновите `bar.js` в функции `handleOrderSubmit`:

```javascript
const orderData = {
    // ...существующие поля
    phone: formData.get('phone').trim(),
};
```

## 📱 Мобильная версия

Все файлы уже адаптированы для мобильных устройств с помощью:
- Flexbox и Grid Layout
- Media queries для разных размеров экранов
- Touch-friendly кнопки и интерфейс

## 🐛 Отладка

### Проверка загрузки меню

Откройте консоль браузера (F12) и проверьте:
```
Network → barzakaz.json
```

Должен вернуться статус `200 OK`.

### Проверка отправки заказов

В консоли браузера при отправке заказа вы увидите:
```
POST https://bar.pelikan-alakol.kz/api/order
```

Если статус `200` - всё работает!

### Просмотр ошибок

Все ошибки выводятся в консоль браузера:
```javascript
console.error('Ошибка загрузки меню:', error);
```

## 📚 Дополнительные ресурсы

- [INTEGRATION.md](../docs/INTEGRATION.md) - Полная инструкция по интеграции
- [QUICKSTART.md](../docs/QUICKSTART.md) - Быстрый старт
- [README.md](../docs/README.md) - Документация бота

## 💡 Советы

1. **Тестируйте локально** перед загрузкой на production
2. **Используйте валидатор JSON** для проверки `barzakaz.json`
3. **Оптимизируйте изображения** (рекомендуемый размер: 400x300px, формат: JPG/WebP)
4. **Добавьте Google Analytics** для отслеживания заказов
5. **Регулярно обновляйте меню** - клиенты любят новинки!

## ❓ FAQ

**Q: Можно ли использовать другой формат данных вместо JSON?**  
A: Да, но тогда нужно будет изменить функцию `loadMenuData()` в `bar.js`.

**Q: Как добавить скидки на блюда?**  
A: Добавьте поле `discount` в JSON и модифицируйте отображение цены в `bar.js`.

**Q: Можно ли добавить модификаторы (размер, топпинги)?**  
A: Да, но это потребует значительной модификации кода. См. раздел "Кастомизация".

**Q: Поддерживается ли несколько языков?**  
A: Пока нет, но можно добавить с помощью i18n библиотеки.

---

**Готово?** Перейдите к [INTEGRATION.md](../docs/INTEGRATION.md) для полной инструкции по установке!
