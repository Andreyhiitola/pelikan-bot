# ⚡ ШПАРГАЛКА: БЫСТРОЕ ОБНОВЛЕНИЕ

## 📦 Что нужно скопировать на сервер:

```
✅ analytics_handler.py       (НОВЫЙ)
✅ analytics_commands.py      (НОВЫЙ)
✅ requirements.txt           (ОБНОВЛЕННЫЙ)
✅ Dockerfile                 (ОБНОВЛЕННЫЙ)
```

---

## 🔧 На сервере выполнить:

```bash
# 1. Обновить .env (добавить SMTP)
nano .env
# Добавить:
# SMTP_SERVER=smtp.mail.ru
# SMTP_PORT=587
# SMTP_USER=your_email@mail.ru
# SMTP_PASSWORD=пароль_приложения_mail_ru
# REPORT_EMAIL=regsk@mail.ru

# 2. Обновить bot.py (добавить 3 строки)
nano bot.py
# Импорты:
#   from analytics_handler import setup_scheduler
#   from analytics_commands import analytics_router
# Роутер:
#   dp.include_router(analytics_router)
# В main():
#   scheduler = setup_scheduler(bot)
#   scheduler.start()

# 3. Пересобрать и запустить
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 4. Проверить
docker logs -f pelikan-bot
# Должно быть: "✅ Планировщик отчетов запущен"

# 5. Тестировать
# В Telegram: /test_report
```

---

## 🎯 Результат:

✅ Каждый день в 8:00 → отчет в Telegram + Email
✅ Команда `/analytics` → выбор периода
✅ Команда `/test_report` → моментальная отправка

---

## 📧 Настройка Mail.ru:

1. Mail.ru → Настройки → Пароль и безопасность
2. Пароли для внешних приложений → Создать
3. Скопировать пароль → вставить в .env как SMTP_PASSWORD

**НЕ используйте основной пароль от почты!**
